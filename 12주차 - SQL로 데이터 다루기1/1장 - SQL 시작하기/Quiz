1. 데이터 베이스는 여러 사람이 공유해 사용할 목적으로 통합하여 관리되는 데이터이 모음을 말한다
   SQL (Structured Query Language)는 데이터 베이스에 접근하고 조작하기 위한 표준 언어이다

2. 하나 이상의 테이블로 이루어지며, 서로 연결된 데이터를 가지고 있고
   sql을 통해 제어가 가능한 데이터베이스는 관계형 데이터베이스 이다

3. SQL에서 내가 원하는 데이터만 검색하기 위해 검색하고자 하는 데이터의 조건을
  설정할 수 있는 명령은 WHERE 이다

4. A NOT IN B 는 B에 A가 포함되지 않은 값을 말한다
   A가 B가 아닌 값은 A != B 로 작성한다

------------------------------------------------------------------


SQL 시작하기
SQL의 세계로 오신 여러분을 환영합니다! 😊
시작이 반이라는 말이 있는 만큼, SQL로 데이터 다루기 I의 1장을 수강함으로써 데이터 다루기의 큰 첫 발을 내딛게 되었네요!
다음으로 넘어가기 전 기초를 탄탄히 다지기 위해 이번 장에서 배운 내용을 복습해 봅시다.



1. SQL이란?
SQL이란 Structured Query Language의 약자로, 데이터베이스에 접근하고 조작하기 위한 표준 언어입니다.
SQL은 여러 개의 연결된 테이블로 이루어진 관계형 데이터베이스를 제어할 수 있게 해줍니다.
모든 테이블은 고유의 이름을 가지고 있으며, 각 테이블은 컬럼(Column)과 레코드(Record)로 구성되어 있습니다.



2. 테이블에서 데이터 검색하기
Point I
SELECT: 검색할 컬럼을 지정합니다.
FROM: 원하는 테이블을 지정합니다.

SELECT title, author 
--검색할 컬럼

FROM book; 
--테이블
Point II
검색할 데이터에 *을 입력하면 모든 데이터를 가져옵니다.

SELECT * 
FROM book; 
Point III
DISTINCT: 중복된 행을 제거하고 보여줍니다.

SELECT DISTINCT title, author

FROM book;
명령어 뒤에 2개 이상의 컬럼을 적으면, 한 쪽 컬럼에 중복이 있어도 다른 쪽 컬럼의 값이 다르면 다르게 취급합니다.


3. 조건을 추가하여 검색하기
WHERE: 검색하고자 하는 데이터의 조건을 설정합니다.

SELECT * FROM book
WHERE title = '돈키호테'; --조건


4. 여러 개의 조건 추가하기
여러 개의 조건을 만족하는 데이터를 검색하기 위해 연산자를 사용합니다.

Point I


SELECT * FROM score
WHERE korean >= 90; --조건
score 테이블에서 국어 성적이 90 이상인 값 검색
Point II


SELECT * FROM score
WHERE korean >= 90 OR math > 80; --조건
score 테이블에서 국어 성적이 90 이상이거나 수학 성적이 80 초과인 값 검색
Point III


SELECT * FROM score
WHERE math BETWEEN 80 AND 90; --조건
score 테이블에서 수학 성적이 80 이상 90 이하인 값 검색


*point 어쩌고는 pdf 참조

--------------------------------------------------------





