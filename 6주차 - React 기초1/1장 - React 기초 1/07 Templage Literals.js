// `"김민수"` 값을 갖는 `username` 변수를 선언하세요.
const name = "김민수";
// `22` 값을 갖는 `age` 변수를 선언하세요.
const age = 22;
// Template Literals를 이용해 `XXX님은 YY세입니다.` 라는 문자열을 출력해보세요. 
// (XXX와 YY는 각각 `username`, `age` 값이 들어가야합니다.)
console.log(`${name}님은 ${age}세입니다.`);