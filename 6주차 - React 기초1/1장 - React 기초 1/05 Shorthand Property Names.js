// `"김민수"` 값을 갖는 변수 `username`을 선언하세요.
const username = '김민수';
// `21` 값을 갖는 변수 `age`를 선언하세요.
const age = 21;
// `Shorthand property names` 표현을 이용하여 `username`과 `age`를 갖는 객체를 생성합니다.
const person1 = { username, age };
// `console.log`를 이용하여 방금 만든 객체를 출력합니다.
console.log(person1);
